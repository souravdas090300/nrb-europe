export interface Dictionary {
  nav: {
    home: string
    articles: string
    about: string
    contact: string
  }
  home: {
    title: string
    description: string
  }
  footer: {
    description: string
    quickLinks: string
    followUs: string
    rights: string
  }
}
