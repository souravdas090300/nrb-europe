/**
 * Custom document actions for the editorial workflow
 * These actions appear in the document menu based on user role and document status
 */

export const customDocumentActions = (prev: any[]) => {
  // For now, return default actions
  // Custom actions can be implemented with proper Sanity context later
  return prev;
};

